{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Full-length 16S/18S HiFi benchmarking pipeline",
  "description": "Parameter schema for the reproducible thesis benchmarking workflow.",
  "type": "object",
  "properties": {
    "mode": {
      "type": "string",
      "enum": [
        "benchmarking",
        "recommended"
      ],
      "default": "benchmarking",
      "description": "Top-level workflow mode for this thesis."
    },
    "input": {
      "type": "string",
      "format": "file-path",
      "exists": true,
      "pattern": "^\\S+\\.(tsv|csv)$",
      "description": "Sequencing samplesheet with fastq paths."
    },
    "outdir": {
      "type": "string",
      "default": "results",
      "description": "Directory where pipeline results will be written."
    }
  },

  "required": [
    "mode",
    "input"
  ]
}