params {
  mode = 'benchmarking'
  outdir = 'results'
}

outputDir = params.outdir

profiles {
    local {
        process.executor = 'local'
    }
}

// Nextflow plugins
plugins {
    id 'nf-schema@2.7.3'
}
